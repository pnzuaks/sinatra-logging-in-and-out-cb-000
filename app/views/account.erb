<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if is_logged_in? %>
      <%= <div class="user_info" id="user_info">
        <h2>"Welcome, <%=@user.username%>!"</h2>
        <h4> "Your account balance is <%=@user.balance%>"</h4>
        <a href="/logout">Log Out</a>
      </div> %>
      <% else %>
      <%= <a href="/error">Log In</a>%>
    <% end %>
  </body>
</html>
